{
    "name": "file for deploying on cyclic",
    "version": "0.1.0",
    "private": true,
    
    "scripts": {
      "start": "cd client && npm start && cd .. && cd server && npm start",
      "build": "cd client && npm run build && cd .. && cd server && npm run build",
      "test": "react-scripts test",
      "eject": "react-scripts eject"
    }
  }
  